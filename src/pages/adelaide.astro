---
import Layout from '../layouts/Layout.astro';
import FactBox from '../components/FactBox.astro';
import TrustSignals from '../components/TrustSignals.astro';
import { getCollection } from 'astro:content';
import { BRAND } from '../utils/brand';
import { formatMigration, MIGRATION_DATA_PERIOD } from '../utils/migration-stats';
import { getPopularRoutesForCity } from '../utils/routeDiscovery';

import { getContentDate } from '../utils/fileDates';
import { fileURLToPath } from 'url';

// Get content modification date
const currentFile = fileURLToPath(import.meta.url);
const pageDate = getContentDate(currentFile);

const allRoutes = await getCollection('routes');
const cityName = 'Adelaide';
const popularRoutes = getPopularRoutesForCity(allRoutes, cityName, 25);

const pageFacts = {
  service: 'Interstate backloading',
  location: 'Adelaide, South Australia',
  coverage: [
    'Adelaide CBD',
    'Gawler',
    'Murray Bridge',
    'Mount Barker',
    'Port Adelaide',
    'Elizabeth',
  ],
  population: '1.4 million',
  typical_transit_to_melbourne: '2-3 business days',
  typical_transit_to_sydney: '3-4 business days',
  typical_transit_to_perth: '3-5 business days',
  typical_savings: '40-60%',
  transit_insurance_included: true,
};
---

<Layout
  title="Backloading Adelaide | Interstate Removals SA | Moving Again"
  description="Adelaide backloading and interstate removals. Save up to 60% on moves to/from South Australia. Weekly services to Melbourne, Sydney & Perth. Transit..."
  schemaType="LocalBusiness"
  schemaData={{
    name: 'Moving Again - Adelaide',
    address: {
      '@type': 'PostalAddress',
      addressLocality: 'Adelaide',
      addressRegion: 'SA',
      addressCountry: 'AU',
    },
    areaServed: 'South Australia',
    priceRange: '$',
  }}
  modifiedDate={pageDate}
>
  <section class="bg-gradient-to-br from-brand-dark to-gray-900 text-white py-20">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto text-center">
        <span
          class="inline-block px-4 py-1 mb-6 rounded-full bg-brand-yellow/20 text-brand-yellow font-bold uppercase tracking-wider text-sm"
        >
          South Australia Hub
        </span>
        <h1 class="text-4xl md:text-6xl font-black mb-6">Backloading Adelaide</h1>
        <p class="text-xl text-gray-300 mb-8 max-w-2xl mx-auto">
          South Australia's capital and central hub for east-west routes. Save up to 60% with
          backloading.
        </p>
        <a
          data-brain-track="quote"
          href="https://removalistquotes.movingagain.com.au/quote/household"
          class="inline-flex items-center justify-center gap-2 py-4 px-8 bg-brand-red text-white font-bold rounded-brand text-lg hover:bg-brand-accent transition-colors shadow-lg"
        >
          Get an Adelaide Quote →
        </a>
        <div class="flex flex-wrap justify-center gap-6 mt-10 text-sm text-gray-400">
          <div class="flex items-center gap-2">
            <span class="text-green-400">✓</span><span
              >{BRAND.yearsInBusiness} Years Experience</span
            >
          </div>
          <div class="flex items-center gap-2">
            <span class="text-green-400">✓</span><span>Transit Insurance Included</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="py-12 bg-white border-b">
    <div class="container mx-auto px-4">
      <div class="grid md:grid-cols-4 gap-8 max-w-5xl mx-auto text-center">
        <div>
          <div class="text-4xl font-black text-brand-red mb-2">1.4M</div>
          <p class="text-sm text-gray-600">Adelaide Population</p>
        </div>
        <div>
          <div class="text-4xl font-black text-orange-500 mb-2">{formatMigration('SA')}</div>
          <p class="text-sm text-gray-600">Net Migration ({MIGRATION_DATA_PERIOD})</p>
        </div>
        <div>
          <div class="text-4xl font-black text-brand-red mb-2">Weekly</div>
          <p class="text-sm text-gray-600">Services All Capitals</p>
        </div>
        <div>
          <div class="text-4xl font-black text-brand-red mb-2">60%</div>
          <p class="text-sm text-gray-600">Typical Savings</p>
        </div>
      </div>
    </div>
  </section>

  <section class="py-16 bg-gray-50">
    <div class="container mx-auto px-4">
      <h2 class="text-3xl font-bold text-center text-brand-dark mb-4">
        What Routes Are Available from Adelaide?
      </h2>
      <p class="text-center text-gray-600 mb-12 max-w-2xl mx-auto">
        Regular backloading services from Adelaide to all major Australian cities
      </p>
      <div class="grid md:grid-cols-3 gap-6 max-w-5xl mx-auto">
        {
          popularRoutes.fromCity.map((route) => (
            <a
              href={`/${route.data.slugFs}/`}
              class="block bg-white p-6 rounded-xl hover:shadow-lg transition-shadow border border-gray-100 group"
            >
              <div class="text-2xl mb-3">🚛</div>
              <h3 class="font-bold text-brand-dark group-hover:text-brand-red transition-colors text-lg">
                Adelaide → {route.data.destination}
              </h3>
              <div class="mt-3 flex gap-4 text-sm text-gray-500">
                <span>{route.data.transitDays || '3-7 days'}</span>
              </div>
            </a>
          ))
        }
      </div>
    </div>
  </section>

  <section class="py-16 bg-white">
    <div class="container mx-auto px-4">
      <h2 class="text-3xl font-bold text-center text-brand-dark mb-4">
        What Routes Are Available to Adelaide?
      </h2>
      <p class="text-center text-gray-600 mb-12 max-w-2xl mx-auto">
        Moving to Adelaide? Regular services from all major cities
      </p>
      <div class="grid md:grid-cols-3 gap-6 max-w-5xl mx-auto">
        {
          popularRoutes.toCity.map((route) => (
            <a
              href={`/${route.data.slugFs}/`}
              class="block bg-white p-6 rounded-xl hover:shadow-lg transition-shadow border border-gray-100 group"
            >
              <div class="text-2xl mb-3">🚛</div>
              <h3 class="font-bold text-brand-dark group-hover:text-brand-red transition-colors text-lg">
                {route.data.origin} → Adelaide
              </h3>
              <div class="mt-3 flex gap-4 text-sm text-gray-500">
                <span>{route.data.transitDays || '3-7 days'}</span>
              </div>
            </a>
          ))
        }
      </div>
    </div>
  </section>

  <section class="py-16 bg-gray-50">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto">
        <h2 class="text-3xl font-bold text-brand-dark mb-6">
          What Routes Are Available from Adelaide?
        </h2>
        <div class="prose prose-lg">
          <p>
            Adelaide's position as Australia's gateway between east and west makes it ideal for
            <a href="/backloading/" class="text-brand-red hover:underline">backloading</a>. Every
            truck crossing the Nullarbor to or from
            <a href="/perth/" class="text-brand-red hover:underline">Perth</a> passes through Adelaide—creating
            regular opportunities for shared loads.
          </p>
          <p>
            From <a href="/melbourne/" class="text-brand-red hover:underline">Melbourne</a>,
            Adelaide is just 730km—an easy 1-2 day transit. For moves heading to
            <a href="/sydney/" class="text-brand-red hover:underline">Sydney</a> or
            <a href="/brisbane/" class="text-brand-red hover:underline">Brisbane</a>, we coordinate
            through our eastern hub network.
          </p>
          <p>
            We quote by <strong>cubic meter</strong> rather than truckload, so whether you're moving a
            few key items or an entire house, you pay fairly for the space you use. With {
              BRAND.yearsInBusiness
            } years of experience, transit insurance is included on all moves.
          </p>
        </div>
      </div>
    </div>
  </section>

  <FactBox facts={pageFacts} />

  <!-- Security Trust Signals -->
  <TrustSignals />

  <section class="py-20 bg-gradient-to-r from-brand-red to-brand-accent">
    <div class="container mx-auto px-4 text-center">
      <h2 class="text-3xl font-bold text-white mb-6">Ready to Move from Adelaide?</h2>
      <p class="text-white/80 mb-8 max-w-xl mx-auto">
        Get an instant quote for your Adelaide interstate move.
      </p>
      <a
        data-brain-track="quote"
        href="https://removalistquotes.movingagain.com.au/quote/household"
        class="inline-block py-4 px-10 bg-white text-brand-red font-bold rounded-brand text-lg hover:bg-gray-100 transition-colors shadow-lg"
        >Get Your Free Quote</a
      >
    </div>
  </section>
</Layout>
